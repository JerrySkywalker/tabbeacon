# TabBeacon v0.7.3 execution roadmap

## Status

**COMPLETE / PUBLICLY RELEASED / LONG-TERM DOGFOOD.**

The v0.7.3 Discoverability, Demo & Distribution Polish train is publicly closed
out. Runtime/provider behavior remained unchanged, and the repository has
entered long-term dogfood with no active feature development.

```text
ADMISSION_BASE_MAIN=63a76bceb6f1710b87aa687bef1f56abf3bf3268
RELEASE_SHA=1485b4dc0fe634a21634adc9ec539deb76beaad2
CURRENT_PUBLIC_RELEASE=v0.7.3
CURRENT_PUBLIC_TARGET=v0.7.3
V073_IMPLEMENTATION=COMPLETE
ACTIVE_FEATURE_DEVELOPMENT=PAUSED
V08_OPTIONS_STATUS=NON_AUTHORITATIVE
ROADMAP_V08_CREATED=false
NEXT_RECOMMENDED_GOAL=LONG_TERM_DOGFOOD_NO_ACTIVE_DEVELOPMENT
```

This is intentionally the last bounded product-maintenance train before an
extended Owner dogfood period. v0.7.3 is not a runtime or provider expansion.

## Product positioning

v0.7.3 adopts a **Codex-led acquisition, provider-truthful product** position.
The preferred public shorthand is:

> **Codex and more**

This phrase is allowed in high-level discovery surfaces because Codex is the
primary user-acquisition path and TabBeacon also has another production provider
(Agy). It must never be used to imply support for deferred providers.

Implementation rules:

```text
POSITIONING_PRIMARY=CODEX_AND_MORE
CODEX_ACQUISITION_PRIORITY=true
SUPPORTED_PROVIDER_TRUTH_REQUIRED_ADJACENT=true
CODEX_SUPPORT=production_capability_based
AGY_SUPPORT=production_exact_1.1.19
CLAUDE_SUPPORT=deferred
OPENCODE_SUPPORT=deferred
UNBOUNDED_PROVIDER_CLAIM=false
```

Candidate discovery copy, subject to final implementation-time length and
rendering checks:

```text
GITHUB_DESCRIPTION=Live status for Codex and more in Windows Terminal — no launcher required.
README_HERO=Live identity and status for Codex—and more—across Windows Terminal tabs, without changing how you launch them.
```

The README/support table must continue to state the exact production support
matrix. `and more` is a positioning phrase, not a compatibility wildcard.

## Release theme

**v0.7.3 — Discoverability, Demo & Distribution Polish**

The admitted scope is exactly:

1. GitHub-native description/topics and deterministic social-preview assets;
2. a deterministic real-Windows-Terminal promotional GIF/poster generated by
   the production presentation path;
3. README/crates.io presentation and package-content polish; and
4. a normal v0.7.3 maintenance release followed by a long dogfood pause.

The following are explicitly out of scope:

```text
RUNTIME_BEHAVIOR_CHANGED=false_expected
PROVIDER_BEHAVIOR_CHANGED=false
NEW_PROVIDER_ADDED=false
WINDOWS_INSTALLER=false
TABBEACON_WINGET_PACKAGE=false
TABBEACON_SCOOP_PACKAGE=false
PATH_MUTATION=false
NATIVE_TAB_ICON_DISPOSITION=NO_GO
CLAUDE_PROVIDER=DEFERRED
OPENCODE_PROVIDER=DEFERRED
CODEX_APP_SERVER=DEFERRED
ROADMAP_V08_CREATED=false
```

## Stable Goal sequence

The historical Goal IDs remain stable even though their filenames still contain
`V072` from the preemption era:

```text
TB-G99   GitHub Discovery Surface
   ↓
TB-G100  Automated Real-WT Promo Demo
   ↓
TB-G101  README & crates.io Distribution Polish
   ↓
TB-G102  v0.7.3 Hardening & Public Release
   ↓
LONG_TERM_DOGFOOD_NO_ACTIVE_DEVELOPMENT
```

All target-version wording in retained G99-G102 planning material must be
reconciled from the old v0.7.2 promotion target to v0.7.3 before the affected
Goal is accepted.

## Reconciliation gate for PR #100

PR #100 is preserved as historical Train-A evidence. Its desired G99/G100 scope
was represented and strengthened by the accepted v0.7.3 Train A in PR #108, so
PR #100 was closed without merge.

```text
PR100_REMOTE_HEAD_AT_READMISSION=4731a3ffbca643a4e3d3afcd3b61f1d849eaa434
PR100_MERGED=false
PR100_STATE=CLOSED_SUPERSEDED
PR100_SUPERSEDED=true
PR100_SUPERSEDED_BY_PR=108
PR108_MERGE_SHA=9215500cb3b0a9ef183c9c096a4bdde1b749da5b
```

The successor retained the social-preview and generator intent, replaced the
old v0.7.2 discovery note with the accepted v0.7.3 research, and added the
stricter exact-owned Windows Terminal lifecycle plus verified demo assets. The
old branch remains available only through Git history.

## G99 — GitHub Discovery Surface

Purpose: a new visitor should understand the product in roughly ten seconds.

Required work:

- finalize the Codex-led repository description;
- select 6-10 accurate GitHub Topics;
- preserve exact support truth near any `Codex and more` phrase;
- reconcile/generate `1280x640` TabBeacon-owned social-preview SVG and PNG;
- validate no external font, remote image, active content, deferred-provider
  overclaim, or private content;
- apply description/topics through supported APIs/CLI;
- use Owner Settings UI only for social-preview upload if GitHub still has no
  supported write API.

Expected topics should be revalidated at implementation time. Historical
candidates include:

```text
coding-agents
codex-cli
ai-coding
windows-terminal
terminal
cli
rust
windows
```

Exit gate:

```text
DESCRIPTION_TRUTHFUL=true
DESCRIPTION_CODEX_LED=true
SUPPORTED_PROVIDER_TRUTH_VISIBLE=true
GITHUB_TOPICS_COUNT=6..10
SOCIAL_PREVIEW_SVG=PASS
SOCIAL_PREVIEW_PNG=PASS
SOCIAL_PREVIEW_DIMENSIONS=1280x640
RUNTIME_BEHAVIOR_CHANGED=false
```

Estimated effective effort: **1-2 h**.

## G100 — Automated Real-WT Promo Demo

Purpose: show real product behavior without using a real model session or
capturing Owner content.

Normative pipeline:

```text
controlled typed fixture
  -> production TabBeacon presentation policy/renderer
  -> one exact-owned stock Windows Terminal window
  -> exact TabItem -> ancestor HWND ownership
  -> exact-window PNG sequence
  -> FFmpeg palette generation/use
  -> committed GIF + poster
```

Reuse the v0.7.2 exact-owned temporary Windows Terminal lifecycle. The earlier
`TOPLEVEL_WINDOW_NAME_ASSUMPTION_INVALID` path is retired.

Required outputs:

```text
docs/assets/demo/tabbeacon-demo.gif
docs/assets/demo/tabbeacon-demo-poster.png
```

Recommended synthetic aliases remain `API`, `WEB`, and `DOCS`. The visible
sequence should truthfully demonstrate Ready/Working/ResultReady/attention
semantics produced by the normal presentation policy.

Media contract:

```text
PROMO_REAL_WINDOWS_TERMINAL=true
PROMO_REAL_TABBEACON_RENDERER=true
PROMO_REAL_MODEL_SESSION=false
NO_DESKTOP_CAPTURE=true
FPS=10
DURATION_SECONDS=8..12
TARGET_WIDTH=960..1100
TARGET_SIZE_MIB<=4_preferred
HARD_LIMIT_MIB<=6
PRIVATE_CONTENT_VISIBLE=false
OWNER_USERNAME_VISIBLE=false
PRIVATE_PATH_VISIBLE=false
UNRELATED_WINDOW_VISIBLE=false
```

Temporary-window contract:

```text
TEMP_WINDOWS_CREATED=1_expected
TEMP_WINDOWS_CLOSED=TEMP_WINDOWS_CREATED
OWNED_TEMP_WT_REMAINING=0
OWNER_WINDOWS_CLOSED=0
BROAD_WINDOWS_TERMINAL_KILL=false
```

The only newly admitted media encoder remains FFmpeg installed/resolved through
Microsoft Winget package `Gyan.FFmpeg`; it is not vendored or redistributed.

Estimated effective effort: **3-5 h**.

## G101 — README & crates.io Distribution Polish

Purpose: make the normal user path simple while keeping release verification
reproducible.

Primary installation contract:

```powershell
cargo install tabbeacon
tabbeacon setup
codex
```

The normal Quick Start must not require a version pin or `--locked`.

Exact release verification remains:

```powershell
cargo install tabbeacon --version 0.7.3 --locked
```

Required work:

- place the real-WT demo high enough in the README to communicate value quickly;
- use the Codex-led `Codex and more` positioning without hiding the exact
  Codex/Agy support table;
- maintain English/zh-CN parity;
- validate GitHub and crates.io README rendering/links;
- audit package metadata and `cargo package --locked` contents;
- keep large marketing assets and evidence out of the `.crate` archive.

Required package exclusions:

```text
PROMO_GIF_IN_CRATE=false
SOCIAL_PREVIEW_IN_CRATE=false
PROMO_BUILD_EVIDENCE_IN_CRATE=false
FFMPEG_BINARY_IN_CRATE=false
```

Estimated effective effort: **1.5-2.5 h**.

## G102 — v0.7.3 Hardening & Public Release

Purpose: prove the public-surface changes do not alter runtime/provider behavior,
then release `0.7.3`.

Required release transition:

```text
0.7.2 -> 0.7.3
```

Release notes should describe only:

1. GitHub discoverability/social preview;
2. deterministic real-Windows-Terminal demo; and
3. README/crates.io distribution polish.

Required gates include:

```text
cargo fmt --check
cargo test --all-targets --locked
cargo clippy --all-targets --all-features --locked -- -D warnings
cargo package --locked
docs/media/package checks
hosted exact-head CI
independent review
```

Public artifacts after explicit G102 release authorization:

```text
crates.io tabbeacon 0.7.3
immutable v0.7.3 tag
GitHub Release v0.7.3
Windows x64 ZIP + SHA256 sidecar
```

Fresh consumers:

```text
cargo install tabbeacon
cargo install tabbeacon --version 0.7.3 --locked
fresh GitHub ZIP/hash/version consumer
```

Estimated effective effort: **3-4 h**.

## PR topology

Preferred structure:

```text
Planning PR  — v0.7.3 re-admission and execution docs
Train A PR   — G99 + G100 discovery/media work (PR #100 if safely reconciled,
               otherwise a clean successor)
Train B PR   — G101 README/crates.io/package polish
Release PR   — G102 v0.7.3 release transaction
Closeout PR  — metadata-only current-truth update after public verification
```

G99 and G100 are intentionally grouped because they share the public visual
surface and existing PR #100 evidence. G101 stays separate because package and
README risk is different from exact-window media capture.

## First 12-hour unattended train

The first long Implementer train should target:

```text
planning PR validation/merge
  -> PR #100/local-recovery reconciliation
  -> G99 complete
  -> G100 complete
  -> G101 as far as safely possible
  -> exact-head CI/review/merge for completed trains
  -> optional G102 release-candidate preparation only
```

It must not publish v0.7.3 unless the active Goal explicitly carries public
release authorization. Social-preview Settings UI upload is non-blocking if the
validated final PNG exists and GitHub still lacks a supported write API.

## Effort

```text
READMISSION_AND_RECONCILIATION=1..2 h
G99=1..2 h
G100=3..5 h
G101=1.5..2.5 h
G102=3..4 h
BEST_CASE_TOTAL=8..10 h
LIKELY_TOTAL=11..15 h
CONSERVATIVE_TOTAL=18..20 h
```

## Long-term dogfood end state

Verified public v0.7.3 closeout established:

```text
CURRENT_PUBLIC_RELEASE=v0.7.3
CURRENT_PUBLIC_TARGET=v0.7.3
V073_IMPLEMENTATION=COMPLETE
ACTIVE_FEATURE_DEVELOPMENT=PAUSED
NO_ACTIVE_FEATURE_DEVELOPMENT=true
V08_OPTIONS_STATUS=NON_AUTHORITATIVE
ROADMAP_V08_CREATED=false
DOGFOOD_MODE=LONG_TERM_DOGFOOD_NO_ACTIVE_DEVELOPMENT
NEXT_RECOMMENDED_GOAL=LONG_TERM_DOGFOOD_NO_ACTIVE_DEVELOPMENT
```

Recommended dogfood duration: **4 weeks minimum, 6-8 weeks preferred**.

During the pause, prioritize observation of Codex upgrades, multi-subagent
sessions, ResultReady/SessionEnd, Ctrl+C/abnormal exits, multi-tab/multi-repo,
upgrade replaceability, stale leases/processes, temporary Windows Terminal
cleanup, diagnostics, and Agy coexistence. New v0.8 work should require a fresh
admission rather than growing out of this maintenance train.
